#include <stdio.h>
#include <stdlib.h>

/*A quantidade de memória necessário para variáveis "estáticas" 
e globais ´edefinida em tempo de compilação; para variáveis "dinamicas"
em tempo de execução.*/

//& = Operador  referencia//
//* = Operador de referencia, o inverso do anterior
//%p = simbolo para demosntrar ponteiros dentro do printf.

int main(){
    /*int n;
    int* p;
    n = 10;
    p = &n;
   
   printf("\nConteudo de p = %p", p);
   printf("\nEndereco de p = %p", &p);
   printf("\nconteudo em que p esta apontando = %d", *p);*/

   /*  
   sintaxe de alocaçao dinamica
   void* malloc(size_t size);
   
   */
    return 0;
}